app.controller('paymentListController', ['$scope', 'authService', '$rootScope', '$window', '$location', 'DATA_URLS', '$http',
function ($scope, authService, $rootScope, $window, $location, DATA_URLS, $http) {

    'use strict';
    //$scope.recordList = [];

    //getList();

    //function getList() {

    //    var promise = $http.post(DATA_URLS.LIST_USERS);

    //    promise.then(function (data) {
    //        if (!data.status || data.status == 200) {
    //            $scope.recordList = data;
    //        }
    //    });
    //}
}]);